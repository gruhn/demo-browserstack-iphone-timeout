# bug demo: web-test-runner + browserstack launcher + iPhone

To reproduce, pull this repository and run:

    export BROWSERSTACK_USERNAME="..."
    export BROWSERSTACK_ACCESSKEY="..."
    npm install
    npm test
